// TASK B: Export a single function as the default export:
// A function `greet` that takes a name and returns "Hello, {name}!"
// (use a template literal)

export default function greet(name) {
  return `Hello, ${name}!`;
}
